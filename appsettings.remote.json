namespace ZEDSafe.App
{
    /// <summary>
    /// Configuration constants for map functionality
    /// </summary>
    public static class MapConstants
    {
        // Map Settings
        public const int DefaultZoomLevel = 10;
        public const int LocationZoomLevel = 19; // User requested default 19
        public const int MinZoomLevel = 2; // Fixed: 10 was too high (white screen)
        public const int MaxZoomLevel = 23; // Extreme zoom (Google Maps style)

        // Zambia Coordinates (Default View)
        public const double ZambiaCenterLat = -13.1339;
        public const double ZambiaCenterLon = 27.8493;
        public const double ZambiaMinLat = -18.0;
        public const double ZambiaMaxLat = -8.0;
        public const double ZambiaMinLon = 22.0;
        public const double ZambiaMaxLon = 34.0;

        // Cache Settings
        public const string DefaultCacheDirectory = "MapCache";
        public const long MaxCacheSizeBytes = 100 * 1024 * 1024; // 100MB

        // Location Settings
        public const double TargetAccuracyMeters = 2.0; // Better accuracy
        public const int LocationTimeoutSeconds = 20; // More time for accuracy
        
        // Animation Settings
        public const int ZoomAnimationDurationMs = 800; // Smooth animation
        public const int IconRotationDurationMs = 1000; // 1 second per rotation
        
        
        // Tile Server URLs
        // Google Hybrid (Satellite + Labels) for best clarity and "Map data not available" fix
        public const string EsriSatelliteUrl = "https://mt1.google.com/vt/lyrs=y&x={x}&y={y}&z={z}";
        public const string UserAgent = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36";

        // UI Settings
        public const double LocationMarkerScale = 0.8;
        public const int LocationMarkerAlpha = 200;
        public const int LocationMarkerOutlineWidth = 3;

        // Button Colors
        public const string ButtonEnabledColor = "#1A73E8"; // Blue when enabled
        public const string ButtonDisabledColor = "#BDBDBD"; // Gray when disabled
    }
}